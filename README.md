# 🍽️ Restaurant Food App – Backend (Node.js + Express)

This is the backend codebase for a Restaurant Food Application.  
It includes a clean MVC folder structure with routes, controllers, models, configuration files, and the main server setup.

---

## 📁 Project Structure

config/ # Database configuration & environment setup
controllers/ # All business logic for handling requests
models/ # Database models / schemas
routes/ # API route definitions
server.js # Entry point of the backend server
package.json # Project metadata & dependencies
.gitignore # Ignored files for Git

yaml
Copy code

---

## 🚀 Features

- Modular **MVC architecture**
- Separate **routes**, **controllers**, and **models**
- Centralized **database config**
- Environment variable support using `.env`
- RESTful API structure
- Clean and scalable backend setup

---

## 🛠️ Tech Stack

- **Node.js**
- **Express.js**
- **MongoDB + Mongoose** (if used in your config)
- **dotenv**
- **Nodemon** (for development)

---

## 🔧 Installation

Clone the repository:

```bash
git clone https://github.com/suprasan07/backend-restaurant-food-app.git
